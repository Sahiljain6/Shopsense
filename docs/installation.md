# Installation Guide

Install Docker, copy `.env.example` to `.env`, then run `docker compose up --build`. Seed local data with `python scripts/seed.py` from an environment where backend requirements are installed.

'use client';import {useEffect,useState} from 'react';import {history,login,register} from '../../lib/api';
export default function History(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{try{let r=await login('demo@shopsense.dev','Password123');localStorage.setItem('token',r.access_token)}catch{let r=await register('demo@shopsense.dev','Password123','Demo User');localStorage.setItem('token',r.access_token)}setRows(await history())})()},[]);return <main className="p-8"><h1 className="text-3xl font-bold">Chat History</h1><div className="mt-6 space-y-3">{rows.map(r=><div key={r.id} className="rounded-xl bg-slate-900 p-4"><b>{r.role}</b><p>{r.content}</p></div>)}</div></main>}

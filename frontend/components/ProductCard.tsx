'use client';
import type {Product} from '../lib/api';import {saveWishlist} from '../lib/api';
export function ProductCard({product,onPick}:{product:Product;onPick?:(id:number)=>void}){return <article className="rounded-2xl border border-slate-800 bg-white/80 p-4 text-slate-950 shadow-xl dark:bg-slate-900 dark:text-slate-100"><img src={product.image_url} alt={product.name} className="h-36 w-full rounded-xl object-cover"/><h3 className="mt-3 font-semibold">{product.name}</h3><p className="text-sm text-slate-500">{product.brand}</p><p className="mt-2 font-bold">{product.currency} {product.price.toLocaleString()}</p><p className="text-sm">★ {product.rating} · Stock {product.stock}</p><p className="mt-2 line-clamp-2 text-sm text-slate-500">{product.description}</p><div className="mt-3 flex gap-2"><button onClick={()=>saveWishlist(product.id)} className="rounded-lg bg-pink-600 px-3 py-2 text-sm text-white">Wishlist</button>{onPick&&<button onClick={()=>onPick(product.id)} className="rounded-lg bg-indigo-600 px-3 py-2 text-sm text-white">Compare</button>}</div></article>}

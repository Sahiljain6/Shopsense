import type { Product } from "../lib/api";
export default function ProductCard({ product }: { product: Product }) { return <article className="rounded-xl border bg-white p-4 shadow-sm"><img src={product.image_url} alt="" className="mb-3 h-32 w-full rounded object-cover" /><h3 className="font-semibold">{product.name}</h3><p className="text-sm text-slate-600">{product.brand} · {product.rating}/5</p><p className="font-bold">{product.currency} {product.price}</p><p className="text-sm">{product.stock > 0 ? `${product.stock} available` : "Out of stock"}</p></article>; }
